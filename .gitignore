# ───────────────────────────────────────────────────────────────
# 🔐  Secret environment files
#     ‒ Ignore every real .env but keep the template example.env
# ───────────────────────────────────────────────────────────────
# Top-level or nested files named exactly ".env"
.env
**/.env

# Dot-env files with extensions, e.g. .env.dev, .env.local
.env.*
**/.env.*

# Any file that simply ends in ".env"
*.env
**/*.env

# ► EXCEPTIONS — keep templates or samples under version control
!example.env
!**/example.env
# ───────────────────────────────────────────────────────────────


# ───────────────────────────────────────────────────────────────
# 🐍  Python artefacts
# ───────────────────────────────────────────────────────────────
# Virtual-environment directories
venv/
env/
myenv/

# Compiled byte-code / caches
*.pyc
__pycache__/
**/__pycache__/
*/_pycache_/       # older layout

# Local Python version indicator
.python-version


# ───────────────────────────────────────────────────────────────
# 🚀  Project-specific build / run outputs
# ───────────────────────────────────────────────────────────────
# Airflow logs
airflow/logs/

# Docker overrides / temporaries
docker-compose.override.yml
docker-compose.override.yml.tmp

# Local run logs or temp dirs
logs/
real_estate_logs/

# Large ML model binaries (add more as needed)
*.pkl
*.gguf
# *.bin        # uncomment to block .bin models


# ───────────────────────────────────────────────────────────────
# 💻  IDE / Editor settings
# ───────────────────────────────────────────────────────────────
.vscode/
.idea/
*.sublime-workspace
*.sublime-project


# ───────────────────────────────────────────────────────────────
# 🖥️  OS-specific cruft
# ───────────────────────────────────────────────────────────────
.DS_Store        # macOS
Thumbs.db        # Windows
